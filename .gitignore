# Build artifacts — every package emits into out/
out/
release/
work/
build/

# Compiled / downloaded / generated artifacts, never committed
fex-sysroot-macros.inc
*.rpm
*.src.rpm
*.tar.zst
*.tar.gz
*.tar.xz
*.cpio
*.img
*.so

# Local env-specific recipes (mac-* remote build flow)
Justfile.local

# Staging dir for the carrier image build
ctx/

# ccache (local builds; CI persists it separately)
.ccache/

# Editor noise
*.swp
*.swo
.idea/
.vscode/
.git-worktrees/
